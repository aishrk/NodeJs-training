const insertDoc = () => {
    _db.collection("users").insertOne({
        "username" : "bar",
        "password" : "bar123"
    }, (err, result)=>{
        if(err) console.log(err)
        
    })
}